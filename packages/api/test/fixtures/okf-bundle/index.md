---
okf_version: "0.1"
title: "Root index"
---

# Root index

This file is regenerated by the memory service on every read — it must
never be imported. Its presence here (with an `okf_version` key, mirroring
a real export bundle's root manifest) exercises the import route's
index-skip path.
