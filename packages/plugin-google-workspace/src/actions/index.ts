/**
 * Engine-native plugin export. The `googleWorkspacePlugin: ActionPlugin` is
 * the canonical entry point for the engine's plugin catalog. Covers Drive,
 * Docs, and Sheets.
 */
export { googleWorkspacePlugin } from './actions.js';
