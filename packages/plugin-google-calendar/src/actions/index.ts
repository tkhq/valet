/**
 * Engine-native plugin export. The `googleCalendarPlugin: ActionPlugin` is
 * the canonical entry point for the engine's plugin catalog.
 */
export { googleCalendarPlugin } from "./actions.js";
export { calendarFetch } from "./api.js";
