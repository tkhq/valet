import type { ValetPlugin } from "@valet/engine";

const plugin: ValetPlugin = {
  name: "telegram",
  version: "0.1.0",
  description: "Telegram channel (transport lands in Phase 7)",
};

export default plugin;
