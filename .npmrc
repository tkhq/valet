# Fail pnpm install/run loudly on the wrong Node version (root package.json
# `engines`). The v2 stack needs Node >= 22 (global WebSocket landed in 22);
# under Node 20 tests fail 15 minutes in with "WebSocket is not defined".
engine-strict=true
