# Progress Ledger — Single Binary + CLI (spec #5)

Branch: `feat/single-binary-cli` (worktree). Plan: `docs/plans/2026-07-17-single-binary-cli.md`.

## Status legend
- [ ] not started · [~] in progress · [x] done (reviewed green) · [!] blocked/owed

## Tasks
- [x] Plan written + committed
- [x] T1 — Uniform asset-read seam (kill migration readdir) — commit 53c870cc; both reviews PASS
- [x] T2 — esbuild bundle + inline/copy assets + bundle guard — commit 25ca43d2; both PASS; full single-file bundle (18.4mb) boots under plain node; note: pglite wasm compile needs ~15s at boot (raise sleeps in integration tests)
- [x] T3 — CLI scaffold + config/profiles + precedence — commit cf03a1e7; both PASS. Minor owed: coerce/validate nested config values (serve.port could be a string from hand-edited config) — T5 should coerce when reading.
  Interfaces: dispatch table Record<string,()=>Promise<CommandModule>>; CommandModule.run(args, ctx)->number; CliContext{command,config}; ExitCode{OK=0,Usage=2,GatePending=3,TurnError=4,AuthFailure=5,Unreachable=6}; CliError hierarchy (ConfigError,ProfileNotFoundError,NoInstanceError,AuthError,UnreachableError,ApiError); config loadConfig/saveConfig/configPath; resolveDataDir/resolveInstance (NOTE: the original resolvePort/resolveSandbox were removed in the final-review cleanup — T5's resolveServeSettings owns port/sandbox precedence incl. the 8788 default + docker→local auto-detect; the old resolve.ts pair hardcoded 8787/docker with no auto-detect, a drift trap); output parseGlobalFlags/printJson/printLine/printErr/emitNdjson.
- [x] T4 — HTTP/WS InstanceClient library — commit 8b752114; both PASS.
  Interfaces: new InstanceClient({url,apiKey?}) with health/me/ensureOrchestrator/listSessions/getSession/createSession/listMessages(id,{threadId?,cursor?,limit?})/sendPrompt/listThreads/listDecisions/resolveDecision; streamSession({url,apiKey?,sessionId,fromOffset?,signal?})->AsyncGenerator<WireEvent> (auto-reconnect on last offset); httpToWsUrl. HealthResponse{ok,service,ts,version?} added to wire/types.ts.
- [x] T5 — `valet serve` command + sandbox detect + implicit local profile — both reviews PASS (spec PASS; quality PASS-WITH-FIXES). Fixed the one Important finding (serve.lock check-and-claim was a TOCTOU — replaced with atomic `claimServeLock` using O_EXCL `flag:"wx"`; stale/malformed locks reclaimed) plus 2 minor lock nits (exit handler registered before boot so an in-boot process.exit still drops the lock; removeLock only deletes a parseable lock that is ours). 33 unit tests pass; api typecheck clean.
  Interfaces: `startServer(): Promise<ServerHandle>` in main.ts (no import side effects; direct-entry guard `import.meta.url===entryHref && /\/main\.(ts|js|mjs)$/` — false in bundle where cli.ts is entry); ServerHandle{close(),port,backend}. serve.ts exports resolveServeSettings(input)->ServeSettings, upsertLocalProfile(config,port), parseLock/isLiveLock/claimServeLock, ServeLock. docker-detect.ts: detectDockerDaemon(probe?)->bool, spawnDockerProbe. Dockerfile.api ENTRYPOINT → `tsx src/cli.ts serve`. serve default port 8788.
  Deviations (minor, recorded): (1) config.serve.authMode is declared but unused — serve derives auth solely from BETTER_AUTH_SECRET; either wire or drop in a later pass. (2) Dockerfile default backend silently flipped docker→local for an UNCONFIGURED container (auto-detect finds no daemon inside); real deploys (k8s chart) always set the backend explicitly, so unaffected.
- [x] T6 — Scriptable commands (sessions/send/gates/status) + health version — both reviews PASS. 45 new tests; api typecheck clean. `/api/health` gains `version`(=VALET_VERSION) + `sandboxBackend` (append-only HealthResponse; no auth/engine change). Commands wired in cli.ts. Each `run` is a thin shell delegating to a pure exported `runX(client, flags)` taking a NARROW client interface (SessionsClient/SendClient/GatesClient/StatusClient) — stubs in tests, no double-casts. `send` injects StreamFn for testable WS. Exit map (send `outcomeToExit`): completed/merged→OK; failed/aborted/superseded→TurnError; decision_gate on our thread→GatePending; stream-ends-before-settle→TurnError. Correlation submission.settled.queueItemId===messageId; delta/gate render filtered by echoed threadId.
  Accepted minor/nit (none blocking, deferred): send has no overall turn-timeout (hang only if the wire contract is violated); health reads env backend not the resolved provider (accurate post-boot); parseSandboxBackend throw-path on health (unreachable — boot fails first on a bad value); send --json dumps all wire events unfiltered by thread (raw dump, deliberate); gates resolve accepts both positional actionId AND --value without a guard. T9 integration validates the real send round-trip.
- [x] T7 — Profile admin (login/logout/instance/config) + reset — both reviews PASS/PASS. 65 new tests (src/cli 180 passed/0 failed); typecheck clean. login verifies via me() BEFORE any write (bad key→AuthFailure, no config written); hidden input via node:readline muted Writable (key never echoed/logged), --api-key flag + non-TTY stdin paths, injectable LoginDeps{makeClient,readSecret}. logout repoints default→sorted-first remaining or clears. instance list --json masks keys to hasKey; use sets default. config scoped to serve.* dot-path only (port int / sandbox / authMode enum coercion; unknown→Usage; never touches profiles). reset reuses serve.ts parseLock+isLiveLock to refuse a LIVE owner (before --yes), --yes bypass, non-TTY-without-yes refuses (no hang).
  Deliberate divergence (safer, documented): reset does a SCOPED wipe — clears pg/, serve.lock, runtime dirs but PRESERVES config.json so `valet login` profiles aren't silently destroyed (plan literal said "wipes dataDir"). Accepted nits: reset.ts re-declares serve.ts's private defaultIsPidAlive (drift-prone; could export+reuse); config port coercion via Number accepts hex/exp (parity with serve.ts coercePort); login --name skips URL validation (fails later at me() as UnreachableError). chat/mcp still notImpl.
- [x] T8 — `valet chat` TUI (lazy readline, zero new deps) — both reviews PASS-WITH-FIXES → fixed. Reuses send.ts renderToolStart/End + outcomeToExit + StreamFn; chat-local numbered gate render/parse (parseGateSelection/renderGatePrompt). Structure: thin run() I/O shell → chatRepl(readLine,runTurn) → chatTurn(injected client/stream/write/readSelection) → gate round-trip drives resolveDecision inline on the SAME generator (events buffered in streamSession's AsyncQueue, nothing dropped). Turn ends only on submission.settled queueItemId===messageId (turn_end/error/close fallbacks); thread-filtered on echoed threadId. 29 chat tests.
  Fixed 2 Important review findings: (1) VERIFIED readline hang — an aborted rl.question left Node's kQuestionCallback set so the NEXT prompt silently dropped its callback and never resolved after a Ctrl-C-during-gate; fix passes {signal} into rl.question so Node cancels its pending callback. (2) run's I/O shell was untested — extracted the readline wrapper to an exported `askLine(rl,prompt,signal?)` and added 5 PassThrough-driven tests incl. the abort-then-next-prompt regression + EOF/already-closed (ERR_USE_AFTER_CLOSE caught → EOF). Deferred minor: credential_request gates can only be answered by action, not free-text value (plan specifies only approval/question). No pty smoke test (pure chatTurn/chatRepl/askLine cover it; avoids node-pty dep + CI flake).
- [x] T9 — CLI integration suite (spawned serve) — QUALITY PASS, SPEC PASS-WITH-FIXES → fixed. File: `packages/api/src/integration/cli.e2e.test.ts` (placed in the `integration` vitest project — un-scrubbed env; NOT the plan's `src/cli/integration/` which would land in the env-SCRUBBED `unit` project with a 10s timeout too short for ~15s PGlite boot). Whole suite opt-in behind `VALET_CLI_E2E=1` (default `pnpm test` spawns nothing); turn cases gate additionally on real ANTHROPIC_API_KEY. Spawns real `tsx src/cli.ts serve --sandbox local` on a free port + unique mkdtemp data dir, polls /api/health, runs the real CLI as child processes resolving the serve's implicit `local` profile. Evidence (VALET_CLI_E2E=1): 6 passed / 2 skipped — status version+skew+sandboxBackend=local, sessions new→list→show (no key needed — session create is a DB insert), exit-code matrix (bogus profile→2, dead port→6, unknown cmd→2), keyless login→instance list(hasKey:false)→logout. Reviewer confirmed no process/dir leak on boot-failure paths and the placeholder-key CANNOT false-pass turn cases (they gate on the ambient key, `??` only falls to the placeholder when unset → turn group skips). Applied nits: runCli watchdog kills hung children; spawnServe timeout awaits child exit before rmSync; runCli drops ambient VALET_INSTANCE so implicit-local resolution isn't overridden.
  serve boot needs a NON-EMPTY ANTHROPIC_API_KEY (main.ts process.exit(1) otherwise); the spawned serve gets a placeholder to clear that gate for the no-turn cases; a real ambient key flows through and enables the turn group.
  OWED (recorded in the test header + here): (1) real-auth login e2e — only stub-auth login/logout covered; real-auth `vlt_` key-mint via better-auth needs a logged-in session (too heavy this pass). (2) gates resolve round-trip — needs a real approval-requesting turn to drive a gate; only `gates list` covered. (3) human-mode `send` (non-json) not exercised (only NDJSON).
- [x] T10 — `valet mcp setup [claude-code] [--print]` — both reviews PASS/PASS. 17 new tests. `--print` emits Claude Code MCP config JSON to stdout (`mcpServers.<name>={type:"http",url:<base>/mcp,headers.Authorization:"Bearer …"}`), caveat to STDERR so stdout stays parseable. `setup claude-code` merges into project-local `./.mcp.json` (preserves other servers + top-level keys; malformed JSON → ConfigError refuse-not-clobber). fs seam injected for tests (real user config never written). Retired notImpl + deleted orphaned _notimpl.ts; ALL 12 COMMANDS entries now real. Applied both nits (err narrowing; dead code + stale comments removed).
  OWED / key deviation (surfaced honestly in a code comment + runtime output, NOT overclaimed): `/mcp` is guarded by better-auth MCP OAuth (`Authorization: Bearer`), NOT the `x-api-key` the other routes use, AND is only mounted when real auth is configured (absent under VALET_LOCAL_AUTH=1). The CLI cannot mint an MCP bearer token yet — the command provisions the config shape + documents the OAuth requirement; the actual OAuth handshake is owed to a future MCP-tool-surface pass. `--token <bearer>` embeds a token if the user already has one.
- [x] T11 — Bun compile spike (3-part gate) — VERDICT: **bundle-only this pass / Node SEA fallback owed** (a legitimate outcome per spec decision 4; not forced). No repo files created; nothing to review (investigation). Evidence (Bun 1.2.17, macOS arm64):
  - Baseline PASS: fresh esbuild bundle `dist/valet-api.mjs` (19.5MB) boots under node, /api/health 200 — this is the SHIPPING artifact.
  - `bun build dist/valet-api.mjs --compile` → runnable 76MB binary, boots + serves health.
  - Gate (a) better-auth login round-trip — PASS: real-auth boot, POST /api/auth/sign-up/email 200 + signed session cookie, replay → GET /api/me 200. scrypt hashing + HMAC cookie signing work under Bun.
  - Gate (b) @hono/node-ws WS streaming — **FAIL (hard)**: against the compiled Bun binary the WS upgrade never fires (no 101, no frame; raw upgrade curl hangs). Same client/protocol against the node bundle → 101 + init frame. Isolates the failure to @hono/node-server+@hono/node-ws `injectWebSocket` under Bun's compiled node:http shim. → STOP per gate order.
  - Gate (c) PGlite kill-test — NOT REACHED (incidental: PGlite wasm loads + serves under compiled Bun at boot).
  - Asset embedding: NOT exercised via Bun --asset; the compiled binary is NOT self-contained (assets resolved via VALET_ASSET_DIR → on-disk dist/assets). Genuine single-file embedding of pglite.wasm/data + web SPA remains unproven and is additional work even if the WS blocker were solved.
  - OWED / follow-up: the only path to revisit compile is the spec's own escape hatch in gate (b) — swap @hono/node-server+@hono/node-ws for Bun.serve native websocket handlers behind the main.ts serve/injectWebSocket seam (a real impl task, not a spike). Deferred.
- [x] T12 — CI distribution — delivered the BUNDLE-in-CI path (native per-platform binaries OWED, consistent with T11's bundle-only verdict). New `.github/workflows/release-cli.yml`: on `v*` tag (or manual dispatch) → pnpm9/Node22/Bun, install, `make generate-registries` (verified deterministic — no diff), build web SPA + api bundle, smoke-boot the bundle under node (poll /api/health), package `valet-api.mjs` + sibling `assets/` + a README into `valet-cli-<tag>-bundle.tgz` (bundle is NOT self-contained — assetBase resolves siblings), upload artifact, and publish a GitHub Release on tag pushes (contents:write). Not runtime-verifiable locally (GH Actions), but all load-bearing local steps (generate-registries, web build, api build, node boot) were proven this session. YAML sanity-checked (no tabs, current action major versions).
  OWED: native single-file binaries (Bun --compile) — deferred per T11 blocker; the workflow ships the node bundle and documents the owed native binaries in the Release body + a header comment.

## Decisions / deviations log
- Phase A does NOT string-inline `.md`/`.sql`; ships them as on-disk sibling assets under `dist/assets/**` resolved via `assetBase()` when `VALET_BUNDLED=1`. True inlining deferred to Phase C (binary). Rationale: tractable, still kills tsx + slims image.
- `GET /api/health` gains `version` (+ maybe `sandboxBackend`) — only api surface change; no engine/auth change.

## Final whole-branch review (opus, cross-cutting)
Verdict: READY-TO-PR. No Critical/functional-Important defects. Confirmed clean: main.ts startServer seam intact (type-only + lazy import; direct-entry guard false in bundle); cli.ts bundle entry no side effects; all commands lazy-imported; ExitCode/CliError mapping uniform; wire-type reuse (only health version+sandboxBackend append-only); migration readdirSync gone; security sound (login hidden+verify-before-write, key masking, mcp placeholder, reset scoped+config.json preserved, atomic serve.lock, 0600/0700); tests honest + e2e double-gated; OWED items deferred with reasons. The one Important finding (dead/divergent resolvePort/resolveSandbox in resolve.ts — a maintenance drift trap, zero runtime effect) was FIXED: removed them + their tests + trimmed SERVE_DEFAULTS to dataDir; serve owns port/sandbox precedence. Accepted minor nits left: reset re-declares serve's private defaultIsPidAlive; version.ts hand-maintained literal (matches package.json); Dockerfile unconfigured-backend flip docker→local (real deploys set it explicitly).

## Finish-line test evidence (Node 22)
- root `pnpm typecheck` (excludes web): clean.
- `pnpm --filter @valet/store-postgres test`: 111 passed / 90 skipped.
- `pnpm --filter @valet/api test` (full): 1343 passed / 19 skipped / **3 failed** — all pre-existing/environmental, none from this arc: 2× `src/routes/messages.abort.test.ts` (known/allowed) + 1× `src/integration/llm-providers.e2e.test.ts` (real OpenAI turn failing on an INVALID ambient OPENAI_API_KEY → 401; the LLM-provider path this arc never touches). Verified each isolated.
- CLI unit suite (`src/cli` + health): green (226+ tests). CLI e2e (`VALET_CLI_E2E=1`): 6 passed / 2 turn-cases skipped (no LLM key).
- Bundle rebuilds clean (19.5MB) + bundle-guard green; compiled Bun binary boots but fails the WS gate (T11 → bundle-only).

---

## Native-binary follow-up arc (branch `feat/bun-native-binary`, stacked on `feat/single-binary-cli`)
Reverses T11's bundle-only verdict. PR #139 (`feat/single-binary-cli`) stays untouched; this lands after it. Plan: `docs/plans/2026-07-18-native-binary-bun-compile.md`.

**Spike (GO):** two throwaway compiled-Bun spikes cleared the two unknowns T11 deferred on. (b) WS: `Bun.serve`+`hono/bun` `createBunWebSocket` → real `101` + streamed frames + clean close under `bun --compile`. (c) PGlite: wasm/data EMBEDDED (via `import … with {type:"file"}`) in a 76MB binary, 500 rows, `kill -9` mid-life, reopen → intact. (a) better-auth already PASSED in T11. Also found: `bun --compile` rewrites `new URL(import.meta.url)` to `/$bunfs` but does NOT embed (readFileSync→ENOENT); `with {type:"file"}` DOES embed and `readFileSync` reads it (sync).

- [x] P1 — Server-runtime adapter seam (Node/Bun) — commit e5b990a7; both reviews PASS/PASS-WITH-FIXES→fixed. `server-adapter{,.node,.bun}.ts`: `isBunRuntime()` selects `Bun.serve`+`hono/bun` (dynamic import — `hono/bun` throws at import under Node, so never in the Node graph) else `@hono/node-server`+`@hono/node-ws` (byte-identical Node path). `createApp` gained an `adapter` param (defaults node); `injectWebSocket` → `startServer(...)→RunningServer`; route handlers unchanged (already take injected `upgradeWebSocket`). Bun-path smoke: boots under `bun`, health 200, WS 101+init frame, injectWebSocket never called. Node bundle still boots. Fixed nit: doc note on Bun `stop(true)` force-close vs Node drain. 5 new tests.
- [x] P2 — Self-contained single-file binary — commit 7c422398; both reviews PASS/PASS-WITH-FIXES→fixed. Only binary assets need embedding (esbuild `inline-assets` already inlines .md/.sql incl. all 7 plugin skills). `make-assets-tar.mjs` (dependency-free USTAR writer) packs `dist/assets/{pglite,web}` (61 files) → `dist/assets.tar`; `compile-entry.mjs` (Bun wrapper) embeds it via `with {type:"file"}`, extracts at boot to a content-hash-keyed temp dir (extract-once, reuse on restart — fixes the reviewer's Important temp-leak finding; atomic rename for concurrent boots; truncation guard added), sets `VALET_ASSET_DIR`+`VALET_BUNDLED`, imports the bundle. ZERO app-code change. Reviewer independently byte-verified all 61 files round-trip (incl. 10MB pglite.wasm). Gate on a 96MB darwin-arm64 binary with `dist/assets` deleted: health 200 + SPA served, WS 101+init, PGlite kill-test data-intact, second boot reuses (no leak). `build:binary` script.
- [x] P3 — Cross-compile + CI + docs — this commit. `compile-binary.mjs` takes `--target`; verified cross-compile from mac builds valid `valet-linux-x64` (ELF x86-64) + `valet-linux-arm64` (ELF aarch64) with assets embedded. `release-cli.yml` rewritten: matrix (macos-14 native darwin-arm64; ubuntu native linux-x64 + cross linux-arm64), each compiles + smoke-boots its native binary (health + SPA) + attaches to the Release. Spec Status + Deviations updated (native-binary deferral reversed). Not runtime-verifiable in CI locally (like T12), but all load-bearing local steps proven.

Residual/owed: ~19MB temp-dir extraction on first boot per build (reused after; true in-memory a future refinement); macOS binaries built on native runner, linux-arm64 cross-built (smoke only on matching runner); Windows via WSL.
