# PR 561-565 Review Fixes Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Fix all actionable review findings on PRs 561, 563, 564, and 565 without rewriting existing commits.

**Architecture:** Add one focused fix commit to each affected PR. Merge each updated lower branch into the next branch so the stack stays linear without force-push. Update each PR's canonical documentation at that PR's tip. Put this plan on the final PR only.

**Tech Stack:** TypeScript, Vitest, pnpm workspaces, Hono, OpenTelemetry, Kubernetes Sandbox CRs, GitHub CLI.

---

### Branch choreography

Use one isolated worktree and temporary local branches. Preserve all existing
commits. Never force-push.

1. Start `codex/pr-stack-storage-fixes-561` at PR 561 head. Push with
   `git push origin HEAD:fix/tkai-385-child-prebuild-flags`.
2. Create `codex/pr-stack-storage-fixes-563` at the original PR 563 head.
   Merge `codex/pr-stack-storage-fixes-561` with `--no-ff`. Push with
   `git push origin HEAD:fix/tkai-401-prebuild-flags-read-hardening`.
3. Create `codex/pr-stack-storage-fixes-564` at the original PR 564 head.
   Merge `codex/pr-stack-storage-fixes-563` with `--no-ff`. Push with
   `git push origin HEAD:fix/tkai-402-adopted-claim-convergence`.
4. Create `codex/pr-stack-storage-fixes-565` at the original PR 565 head.
   Merge `codex/pr-stack-storage-fixes-564` with `--no-ff`. Push with
   `git push origin HEAD:fix/tkai-403-workspace-env-validation`.

Before each push, verify that the remote branch still points to its recorded
original head. Stop if it changed.

### Task 1: Fix PR 561 repository targeting, quantity parsing, and test fixtures

**Files:**
- Create: `packages/api/src/test-helpers/recording-sandbox.ts`
- Modify: `packages/api/src/engine/host.ts`
- Modify: `packages/api/src/engine/host.prebuild-flags.test.ts`
- Modify: `packages/api/src/engine/host.prebuild.test.ts`
- Modify: `packages/sandbox-kubernetes/src/quantity.ts`
- Test: `packages/sandbox-kubernetes/test/quantity.test.ts`
- Test: `packages/api/src/engine/host.prebuild-flags.test.ts`
- Modify: `docs/specs/2026-09-03-sandbox-workspace-fit-design.md`

- [x] Add quantity tests for decimal SI suffixes (`n`, `u`, `m`, empty,
      `k`, `K`, `M`, `G`, `T`, `P`, `E`), binary SI suffixes (`Ki` through
      `Ei`), and signed decimal exponents (`e` or `E`). Positive fractional
      bytes must round up to one byte instead of becoming the disable value.
- [x] Run `pnpm --filter @valet/sandbox-kubernetes test quantity` and confirm the new cases fail.
- [x] Extend `parseStorageQuantity` to accept the Kubernetes quantity forms used by the API while preserving positive fractions.
- [x] Run the quantity suite and confirm it passes.
- [x] Rename `prebuildFlagsTarget` to `primaryGitHubRepoTarget` and update its tests and callers.
- [x] Extract the duplicated recording sandbox classes into `src/test-helpers/recording-sandbox.ts`.
- [x] Update the canonical design for the helper name and accepted quantity semantics.
- [x] Run `pnpm --filter @valet/api test host.prebuild-flags host.prebuild`.
- [x] Commit and push the PR 561 fix.

### Task 2: Fix PR 563 GitHub read semantics and metric placement

**Files:**
- Modify: `packages/api/src/bakes/source-service.ts`
- Test: `packages/api/src/bakes/source-service.test.ts`
- Modify: `packages/api/src/engine/host.ts`
- Test: `packages/api/src/engine/host.prebuild-flags.test.ts`
- Create: `packages/api/src/observability/prebuild-metrics.ts`
- Test: `packages/api/src/observability/prebuild-metrics.test.ts`
- Modify: `packages/api/src/routes/sources.ts`
- Test: `packages/api/src/routes/sources.test.ts`
- Modify: `packages/engine/src/index.ts`
- Modify: `packages/engine/src/metrics.ts`
- Modify: `docs/specs/2026-09-03-sandbox-workspace-fit-design.md`

- [x] Merge the updated PR 561 branch into the PR 563 work branch.
- [x] Add a test that a 2xx non-file payload produces an uncached error.
- [x] Add a host test that a permanently pending read is evicted after the five-second timeout and the next build retries.
- [x] Add a route test that a GitHub upstream failure maps to a typed 502 response whose message names a corrective action.
- [x] Run both tests and confirm they fail for the reviewed behavior.
- [x] Make non-file payloads throw `GitHubApiError` and add corrective text to surfaced GitHub errors.
- [x] Pass an abort signal from the host deadline into the shared contents read so a timeout settles and removes the in-flight entry.
- [x] Move the prebuild outcome type and metric recorder into API observability.
- [x] Add a typed route error response before returning the 502.
- [x] Update the canonical design with failure, timeout, cache, and observability semantics.
- [x] Run `pnpm --filter @valet/api test source-service host.prebuild-flags sources prebuild-metrics`.
- [x] Run `pnpm --filter @valet/engine test policy-workspace-grow`.
- [x] Commit and push the PR 563 fix. Verify that its PR description contains no prohibited dash.

### Task 3: Fix PR 564 adopted-claim observability and documentation

**Files:**
- Modify: `packages/sandbox-kubernetes/src/provider.ts`
- Test: `packages/sandbox-kubernetes/test/provider.test.ts`
- Modify: `packages/engine/src/metrics.ts`
- Modify: `packages/api/src/engine/workspace-prep.ts`
- Modify: `packages/engine/src/sandbox/policy.ts`
- Modify: `docs/specs/2026-09-03-sandbox-workspace-fit-design.md`
- Modify: `docs/prebuild-yaml.md`

- [x] Merge the updated PR 563 branch into the PR 564 work branch.
- [x] Add a provider test that a PVC pre-read failure logs and records an error outcome.
- [x] Run the provider test and confirm it fails.
- [x] Report pre-read failures without blocking sandbox creation.
- [x] Rename the `wait_timeout` grow outcome to `pending` at all producers and in metric documentation.
- [x] Reconcile the canonical design and prebuild schema documentation with adopted-claim convergence.
- [x] Run `pnpm --filter @valet/sandbox-kubernetes test provider workspace-pvc lifecycle manifest quantity`.
- [x] Run `pnpm --filter @valet/api test workspace-prep` and `pnpm --filter @valet/engine test policy-workspace-grow`.
- [x] Commit and push the PR 564 fix.

### Task 4: Finish PR 565 documentation and stack validation

**Files:**
- Modify: `docs/specs/2026-09-03-sandbox-workspace-fit-design.md`
- Add: `docs/plans/2026-09-03-pr-561-565-review-fixes.md`

- [x] Merge the updated PR 564 branch into the PR 565 work branch.
- [x] Document storage environment validation, effective defaults, cap checks, and the deploy floor.
- [x] Run `git diff --check` and `make e2e E2E_ARGS="--only docs-lint"`.
- [ ] Run `pnpm --filter @valet/api test host.prebuild-flags source-service sandbox-backend sources workspace-prep prebuild-metrics`.
- [ ] Run `pnpm --filter @valet/sandbox-kubernetes test provider workspace-pvc lifecycle manifest quantity`.
- [ ] Run `pnpm --filter @valet/engine test policy-workspace-grow`.
- [ ] Run `pnpm typecheck`.
- [ ] Run the full `make e2e` scorecard. Classify every non-green row as an environment failure or a regression.
- [ ] Commit and push the PR 565 fix.
- [ ] Confirm all four PR heads, bases, descriptions, and GitHub checks.
